let assert = require('assert')

describe.skip('#indexOf', () => {
  it.only('should return -1 when the value is not matched', () => {
    assert.equal([1, 2, 3].indexOf(4), -1)
  })

  it.only('dddddddd')
})


